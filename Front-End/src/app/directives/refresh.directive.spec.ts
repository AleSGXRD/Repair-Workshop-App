import { RefreshDirective } from './refresh.directive';

describe('RefreshDirective', () => {
  it('should create an instance', () => {
    const directive = new RefreshDirective();
    expect(directive).toBeTruthy();
  });
});
